---
features:
  - |
    Add new constructors to C++ ``ConstantNode`` and add update the existing
    constructors to accept ``const double*`` rather than just ``double*``.
fixes:
  - |
    Fix constructing ``Constant`` symbols from read-only NumPy arrays.
    See `#296 <https://github.com/dwavesystems/dwave-optimization/issues/296>`_.

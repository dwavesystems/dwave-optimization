---
features:
  - Add Python ``broadcast_shapes()`` function.
fixes:
  - Disallow broadcasting a fixed size array to a dynamic shape with ``BroadcastTo`` symbol.
upgrade:
  - Rename C++ ``broadcast_shape()`` to ``broadcast_shapes()`` for consistency with NumPy.
